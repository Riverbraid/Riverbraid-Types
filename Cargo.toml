[package]
name = "riverbraid-types"
version = "0.1.0"
edition = "2021"
description = "Canonical shared Rust type layer for Riverbraid"

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }
hex = "0.4"

[features]
default = ["std"]
std = ["alloc"]
alloc = []
