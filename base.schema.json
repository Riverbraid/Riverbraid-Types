{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "repo_state": {
      "type": "string",
      "enum": ["active", "parked", "experimental", "deprecated", "archived"]
    },
    "authority_class": {
      "type": "string",
      "enum": ["normative", "verifier", "support", "informative", "none"]
    }
  }
}
